[defaults]
retry_files_enabled = False
host_key_checking = False
inventory = ansible/vars/cluster/inventory/hosts
private_key_file = ansible/vars/cluster/keys/insecure_private_key
remote_user = vagrant
remote_tmp = /tmp/ansible_tasks
